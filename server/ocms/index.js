const { activeMembersReq } = require("./activeMember");
const { gameTypeReq, gameProviderReq } = require("./game");
const { summary } = require("./summary");

module.exports = {
  activeMembersReq,
  gameTypeReq,
  gameProviderReq,
  summary,
};
